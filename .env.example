# ========================================
# CONFIGURAÇÃO DO SISTEMA CNPJ API
# ========================================
# ⚠️ IMPORTANTE: 
# 1. Copie este arquivo para .env
# 2. Preencha TODAS as variáveis
# 3. NUNCA commite o arquivo .env no Git!
# ========================================

# ========================================
# BANCO DE DADOS (OBRIGATÓRIO)
# ========================================
# URL completa do PostgreSQL na VPS
# Formato: postgresql://usuario:senha@host:porta/database
DATABASE_URL=postgresql://seu_usuario:sua_senha_forte@seu_host:5432/cnpj_db

# Alternativa: configuração separada (legacy)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cnpj_db
DB_USER=postgres
DB_PASSWORD=sua_senha_forte_aqui

# ========================================
# SEGURANÇA (OBRIGATÓRIO)
# ========================================
# SECRET_KEY para JWT - GERE UMA CHAVE FORTE!
# Use: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=GERE_UMA_CHAVE_FORTE_AQUI_MINIMO_32_CARACTERES
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# ========================================
# SERVIDOR API
# ========================================
API_HOST=0.0.0.0
API_PORT=8000
API_TITLE=API de Consulta CNPJ
API_VERSION=1.0.0

# ========================================
# CORS (IMPORTANTE PARA PRODUÇÃO!)
# ========================================
# Em DESENVOLVIMENTO: use *
# Em PRODUÇÃO: liste domínios permitidos separados por vírgula
# Exemplos:
#   ALLOWED_ORIGINS=https://seu-dominio.com,https://www.seu-dominio.com
#   ALLOWED_ORIGINS=https://api.seu-dominio.com
ALLOWED_ORIGINS=*

# ========================================
# ETL (OPCIONAL)
# ========================================
DOWNLOAD_DIR=./downloads
BATCH_SIZE=10000
CHUNK_SIZE=50000
NUM_WORKERS=4
MAX_WORKERS=4

# ========================================
# ⚠️ CHECKLIST DE SEGURANÇA PARA PRODUÇÃO:
# ========================================
# [ ] Gerei SECRET_KEY forte (mínimo 32 caracteres)
# [ ] Configurei DATABASE_URL com senha forte
# [ ] Alterei ALLOWED_ORIGINS para domínios específicos
# [ ] Verifiquei que .env está no .gitignore
# [ ] NUNCA commitei o arquivo .env no repositório
# [ ] Rotei a senha do banco após exposição acidental
# [ ] Configurei rate limiting adequado
# [ ] Configurei logs estruturados
# ========================================
